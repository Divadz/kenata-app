# Config des Cloud Functions — copier en `functions/.env` (non commité).
# Email du propriétaire initial du groupe (bootstrap du premier accès owner).
OWNER_EMAIL=ton-email@gmail.com
# Identifiant du groupe unique (doit correspondre à VITE_GROUP_ID côté client).
GROUP_ID=kenata
