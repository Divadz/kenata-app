node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Config backend (secrets) — ne JAMAIS versionner
api/config.php
api/config.keys.php
kenata-keys.php
kenata-config.php

# Scripts de migration jetables
api/_migrate.php

# Exemples de factures (données réelles) — ne pas versionner
Facture_*.pdf
Facture_*.png

# Affiches uploadées (générées à l'exécution)
uploads/

# Scripts locaux (chemins/identifiants spécifiques à la machine)
start-dev.bat
deploy.ps1

# Éditeurs
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
